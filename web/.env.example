# Copy to web/.env and fill in. Next.js loads this file automatically.
# Nothing reads these yet — they are placeholders so the convention exists.

# Public base URL of the web app.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
